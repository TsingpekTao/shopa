# catalog-svc configuration (local environment).
# Start command example:
#   gf run main.go --gf.gcfg.file=config.local.yaml

# HTTP server configuration (OpenAPI/Swagger + optional REST APIs).
server:
  address: ":8004"
  openapiPath: "/api.json"
  swaggerPath: "/swagger"

grpc:
  address: ":9004"
  name: "catalog.svc"
  logStdout: true

# Application identity, used for key prefix and tracing labels.
app:
  env: "local"
  project: "shopa"
  service: "catalog-svc"

# External dependencies.
database:
  default:
    link: "mysql:root:123456@tcp(127.0.0.1:3306)/shopa_catalog_svc?charset=utf8mb4&parseTime=true&loc=Local"

redis:
  default:
    address: "127.0.0.1:6379"
    db: 0
    pass: ""

mq:
  rabbitmq:
    enabled: false
    url: "amqp://guest:guest@127.0.0.1:5672/"
    exchange: "shopa.events"

# Service-to-service addresses (for RPC/http client usage, can be overridden per env).
upstream:
  edgeGateway: "edge-gateway:8000"
  iam: "iam-svc:8001"
  userProfile: "user-profile-svc:8001"

# Object storage / media placeholders.
storage:
  provider: "local"
  endpoint: ""
  accessKey: ""
  secretKey: ""
  bucket: "shopa"

logger:
  level: "all"
  stdout: true
